name: Generate code with ChatGPT
on:
  issues:
    types: [opened, reopened, labeled]
jobs:
  generate-code:
    env:
      GPT_SCRIPT: ./scripts/script.sh
      ISSUE_NUMBER: ${{ github.event.issue.number }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set permissions for script.sh
        run: echo "Set permissions for script.sh" 
          chmod -v +x $GPT_SCRIPT
      - name: Run script.sh
        run: echo "Run script.sh" 
          $GPT_SCRIPT ${{ secrets.GITHUB_TOKEN }} $GITHUB_REPOSITORY $ISSUE_NUMBER ${{ secrets.OPENAI_API_KEY }}
      - name: Upload generated files as artifacts
        run: echo "Upload generated files as artifacts"
      - name: Download artifacts
        run: echo "Download artifacts"
      - name: List files in the artifact
        run: echo "List files in the artifact"
